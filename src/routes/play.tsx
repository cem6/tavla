import Board from '../components/Board'

export default function Play() {

  // implement peer to peer connection in here, pass multiplayer data to board component
  // => need modified board component

  return (
    <div className="flex justify-center items-center min-h-screen">
      <Board />
    </div>
  )
}